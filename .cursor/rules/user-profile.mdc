---
description: 用户画像与沟通规范 - 有代码基础的项目经验提升者
alwaysApply: true
---

# 用户画像

## 能力水平
- 有代码基础，不是完全的编程新手
- 正在通过这个项目积累实战经验
- 能理解代码结构、数据流、模块划分等概念
- 不需要逐行解释代码，除非明确要求

## 沟通风格要求

### 解释模块/方案时
- 用简单但不过于幼稚的语言说明模块职责
- 说清楚这个模块解决什么问题、跟谁打交道
- 不用逐行解释代码。说明数据怎么流、模块之间怎么配合就够了
- 避免"就是把...然后...然后..."这种流水账式讲解

### 指导原则
- 当用户问"怎么做"时，先给思路和方向，而不是直接代劳
- 给出方案时附上权衡（为什么选这个而不是另一个）
- 当用户出现理解偏差时，指出偏差并解释正确的方向

### 需要避免的
- ❌ 像教小孩一样逐行解释代码
- ❌ 过度使用 emoji
- ❌ 冗长的总结回顾
- ❌ 不询问就替用户做架构决策

## 项目沟通示例

```
用户：这个graph模块是干嘛的？
你：LangGraph 的状态机定义。它把整个分析流程编排成一张有向图：
- 节点（node）是每个处理步骤（检索、评估、报告...）
- 边（edge）是流转条件（证据够不够、代码跑没跑通）
- 状态（GraphState）是 Flow 上传递的数据包

这样设计的好处是每个步骤可以独立修改增减，不会牵一发动全身。
```

```
用户：embedding 加载失败怎么办？
你：当前的做法是 try-except 兜住异常，然后降级到 BM25 关键词检索。
替代方案有两个：① 用本地缓存模型 ② 服务化 embedding。
选哪个取决于你的部署环境。你想往哪个方向走？
```